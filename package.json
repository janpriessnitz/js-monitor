{
  "name": "cvmfs-monitor",
  "version": "0.1.0",
  "dependencies": {
    "backend": "file:backend",
    "frontend": "file:frontend",
    "nodemon": "^1.19.1",
    "@babel/node": "^7.5.5"
  },
  "scripts": {
    "backend": "cd backend && npm run server",
    "frontend": "cd frontend && npm run build && serve -s build -l 8080",
    "start": "npm run backend & npm run frontend &"
  }
}
